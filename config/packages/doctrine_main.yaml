# config/packages/doctrine_main.yaml
doctrine:
  orm:
    entity_managers:
      default:
        connection: default
        mappings:
          User:
            is_bundle: false
            type: attribute
            dir: '%kernel.project_dir%/src/User/Domain/Entity'
            prefix: 'App\User\Domain\Entity'
            alias: User